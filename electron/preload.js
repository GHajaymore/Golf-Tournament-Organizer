// Nothing exposed to the renderer yet — the desktop shell just displays the
// same web app the browser and mobile shells do. Add contextBridge APIs
// here if a future feature needs real OS integration (e.g. native file
// export, print dialogs beyond the browser's own).
