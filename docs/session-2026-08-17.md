# Session — 2026-08-17

Twenty-six commits on `build/production-app`. **Nothing pushed, nothing
deployed, no real SMS sent.** Migrations 53, 54 and 55 are applied to the
**dev** database only.

Two halves: finishing the 2026-08-12 audit, then designing and building the
money model from scratch with the product owner.

---

## 1. The audit is now clear

`docs/audit-2026-08-12.md` is the canonical record and was updated in this
session. Everything in its sections 1–4 is `FIXED` except **P2, P3, P4** and
the stroke-play items in §5, which are capability gaps rather than defects.

Closed this session: the ranking comparator, both forfeit tiebreakers, the team
forfeit (refused rather than silently discarded — see below), **D5, D6,
D10–D14**, and the money-game arithmetic.

**Two corrections to the record.** An earlier summary had listed D1–D9 as
closed. Re-reading the source found **D5 and D6 still open**, and both were
fixed here. A summary is not evidence; the code is.

### The team forfeit is a GAP, not a fix

`forfeitMatch` accepted a team id and stored it, and nothing ever read it —
standings aggregate over the player columns, which a team round leaves empty.
An organizer recorded a concession, saw it accepted, and got a result that
counted for nothing. It now **refuses**, which is the honest half until team
match-play standings exist. Do not "fix" this by making it store again.

---

## 2. The money model — designed in conversation, built here

The shape came from the product owner over the session. Recorded because the
reasoning is not recoverable from the code alone.

### Organization kinds (`lib/domain/org-profile.ts`)

Four kinds — `club`, `course`, `community`, `personal` — each declaring what it
MEANS: `sharedRoster`, `ledger`, `seasonPlay`, `ownsCourse`, `tracksCash`.
Everything asks this rather than comparing a string, so adding a kind is one
row and the compiler finds every place that needs an answer.

**A club and a course do not track cash.** The shop takes the entry fee, the
2s pot comes out of it, and the professional pays the winner — the app's job
there is to say who won. This was the product owner's own answer and it is the
flag that decides whether "take their money" and "you owe" appear at all.

### Money modes (`lib/domain/money-mode.ts`)

`none` · `float` · `split`, resolved **tournament → club → kind**. Both stored
columns default to empty meaning "follow the level above", so every existing
organization behaves exactly as it did until somebody chooses.

Per tournament, not per club: a society runs a thirty-pound Sunday roll-up and
a three-day trip with a minibus in the same season.

**Float and split are different questions.** Float money has left the players'
hands (one pot belonging to the tournament); split money has not (somebody
fronted it and is owed). Conflating them is how a settle-up ends up telling a
player they owe the organizer money they paid at signup. The float is
deliberately **not per player** — per-player fee reconciliation is a separate
feature nobody has asked for yet.

### Pot entry modes (`lib/domain/pot-entry.ts`)

`opt-in` (a name is in because somebody put it there) and `opt-out` (everyone
playing is in unless they said otherwise). Per pot, because a club runs both on
the same day: the closest-to-the-pin is on for everybody and the sweep is for
whoever fancies it.

One honest relaxation, confined to opt-out: **a player with no row counts as a
confirmed stake**, because the premise of "everyone is in" is a club that
collects as a matter of course. An organizer who has not been paid marks that
player unpaid.

`excluded` is a second column and not a reuse of `confirmed`: "took himself
out" and "has not paid yet" settle differently.

### Payouts are round-based, and final only

Money is won on a given day by a given card. A running skins position is not an
early view of the answer but a different number that looks like one — the carry
means one hole can take the lot. Rounds finish three ways: every card in, every
match settled, or the organizer closing the tournament.

---

## 3. The pattern worth remembering

**One rule, read in one place.** Three separate defects this session were the
same shape — a second implementation of a rule, disagreeing with the first:

- the ranking comparator vs. the forfeit tiebreakers;
- `loadEventState` fetching matches with **no `orderBy` at all**, so entering a
  score physically moved the row and reshuffled the draw;
- **pot membership read three different ways** — the prizes screen, the ledger
  arithmetic, and the side-bets display. The same contest was worth $165 on one
  screen and $5 on another.

`potMembership` is the single reader now. A fourth place reading entry rows
directly will disagree again.

**Look at the screen.** Almost nothing here was found by a test. The scroll
trap, the tokenless invite, the "0 in Demo Cup" contradiction, the missing sign
out, the match-play round that could never finish, and the scorecard paste
turning a total into a par were all found by opening the page and using it.

---

## 4. What is left

In the order I would take it:

1. **P2** — six log lines in `lib/email.ts` print member email addresses.
   Started, not finished.
2. **P3, P4** — token rotation; the unpublished tee sheet visible to players.
3. **Stroke-play tiebreaks** — two players on 72/72 are separated by seed
   order, silently. A club secretary needs a countback.
4. The capability gaps in §5 of the audit — they need scope decisions.

### Known gaps in what was built

- **No per-player fee reconciliation** for the float ("did all forty pay?").
- **`looksLikePhone` accepts a landline.** Deliberate — a stricter pattern
  loses real entries — but the free plan now requires a mobile. Revisit when
  SMS is switched on, not before.
- **The three metered features** (SMS, card scan, AjAi drafting) are built and
  dark on both plans. See `lib/plans.ts` and the memory note.

### Before deploying

Migrations 53–55, then `scripts/backfill-org-owners.ts --apply`.

---

## 5. Dev-database changes made while verifying

The Demo Cup is no longer pristine. All of it was to make a screen show
something real:

- "Walkthrough Player" added to the field, with a stroke-round card.
- The 8 unstarted matches settled, so the round-robin stage completes.
- A $5 nearest-the-pin (opt-out), won by Walkthrough Player.
- A stroke-play round added, with 6 cards and a $10 net skins pot.
- Three shared expenses, and the tournament switched to `split`.
- 32 members added to the club roster from the field.
- Toughest 9 and Toughest 2 added to the tiebreaker chain.
- 3 matches approved via the new bulk approve.
