name: CI

# Typecheck, test and — the point of this file — actually build.
#
# `next build` shares .next with `next dev`, so building on a machine that has
# the dev server running corrupts it. That has meant the production build was
# effectively never verified locally: every change shipped on the strength of
# tsc and vitest, neither of which sees a server/client boundary violation, a
# bad "use server" export shape, or a static-generation failure.
#
# CI has no dev server, so it can just build. Free on a public repository.

on:
  push:
    branches: ["**"]
  pull_request:
  workflow_dispatch:

# A newer push makes an in-flight run pointless.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    services:
      # Prisma needs a reachable database to generate against and for the
      # build to import the client. A throwaway container is cheaper and more
      # honest than pointing CI at a real one.
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: tourneyhq_ci
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
        ports:
          - 5432:5432

    env:
      # Local to the runner and destroyed with it — never a real credential.
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/tourneyhq_ci
      # A shadow database has to be empty — Prisma replays every migration into
      # it from scratch. Pointing it at the migrated one silently reports no
      # drift, whatever the schema says.
      SHADOW_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/tourneyhq_shadow
      ADMIN_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/postgres
      # Keeps the build from trying to phone home during static generation.
      NEXT_TELEMETRY_DISABLED: "1"

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install
        run: npm ci

      - name: Generate Prisma client
        run: npx prisma generate

      - name: Create shadow database
        run: |
          echo 'CREATE DATABASE tourneyhq_shadow;' > /tmp/shadow.sql
          npx prisma db execute --url "$ADMIN_DATABASE_URL" --file /tmp/shadow.sql

      - name: Check for schema drift
        # Before the real database is touched. Drift means schema.prisma was
        # edited without the matching migration, so the next deploy applies a
        # different database than the one CI tested.
        run: >-
          npx prisma migrate diff
          --from-migrations prisma/migrations
          --to-schema-datamodel prisma/schema.prisma
          --shadow-database-url "$SHADOW_DATABASE_URL"
          --exit-code

      - name: Apply migrations
        # Catches a migration that does not apply to an empty database —
        # the failure mode a local machine never sees, because its database
        # already has every earlier migration in it.
        run: npx prisma migrate deploy

      - name: Typecheck
        run: npx tsc --noEmit

      - name: Test
        run: npx vitest run

      - name: Build
        run: npm run build
